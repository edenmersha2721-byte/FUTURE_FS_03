# In development, leave this blank — Vite proxies /api to the backend (see vite.config.js).
# In production (Vercel), set this to your deployed backend URL, e.g.
# VITE_API_URL=https://luxe-salon-api.onrender.com/api
VITE_API_URL=
